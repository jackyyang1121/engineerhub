# models.py - 定義 search app 的資料模型（目前尚未建立任何模型）
# 可在此檔案新增搜尋紀錄、熱門關鍵字等資料表

from django.db import models

# 目前尚未定義任何 models，範例：
# class SearchHistory(models.Model):
#     user = models.ForeignKey(...)
#     query = models.CharField(...)
#     created_at = models.DateTimeField(...)
